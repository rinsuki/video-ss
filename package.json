{
  "name": "video-ss",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "make-firefox-zip": "yarn build && rm -f firefox.zip && zip firefox.zip manifest.json *.js *.html"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.77",
    "typescript": "^3.2.2"
  }
}
